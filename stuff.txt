class sim

age enum: elementary schooler
	    high schooler
            university student
            adult

job status enum:
	student
	employed
	unemployed

wealth level:
	low
	medium
	high
	filthy rich

education lvl: 

home location: X,y
Family Number:

Job path string:

commercial path string:

Hobby path string

school path string

commute hours

functions:

init(bool movedOrBorn){

if moved(assign new location, set age to adult, assign employement status, calculate employement path nodes, calculate shopping path nodes, assign commute time, calculate hobby path string, assign new family number(this is tricky)

if born(location of parents, set age to 0, 7 years  at home

	